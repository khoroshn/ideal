import React from 'react';
import Footer from './Footer';

const layout = (props) => {
	return (
		<div>
			<div>It is layout {props.name} component {props.age}</div>
			
		</div>
	)
};

export default layout;